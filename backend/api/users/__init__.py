"""
This package contains the users endpoints.

Modules:
    - [`jwt_auth`][backend.api.users.jwt_auth]:
        A handler that handle custom JWT auth.
    - [`models`][backend.api.users.models]:
        A module that contains models of users package.
    - [`serializers`][backend.api.users.serializers]:
        A module that contains serializers of users package.
    - [`urls`][backend.api.users.urls]:
        A module that contains urls of users package.
    - [`views`][backend.api.users.views]:
        A module that contains users views.
"""
